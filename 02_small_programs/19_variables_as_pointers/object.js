let obj = {
  a : 1,
};

obj = {
  b : 2,
};

console.log(obj);

// let obj is a variable at one address, it stores a pointer to the object value
// the object value is at a different address to the variable
// obj variable is reassigned to point to a different address whereby a
// different value is stored

